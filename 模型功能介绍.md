原来逻辑是：

```text
Qwen 第二阶段失败
    ↓
json_parser 从原始中文里扫关键词
    ↓
根据颜色/类别选检测框
```

问题是它容易把参考物当目标。比如：

```text
抓取黄色方块，放到货架中白色长方体的左边
```

原来的兜底逻辑可能看到“白色”“长方体”，就偏向 `white cuboid`。但真正要抓的是 `yellow box`。

改完后逻辑是：

```text
Qwen 第一阶段理解任务
    ↓
输出 target_object = yellow box
    ↓
GroundingDINO 检测 yellow box、white cuboid、shelf
    ↓
Qwen 第二阶段结合检测结果选目标
    ↓
如果第二阶段失败，兜底也优先用第一阶段 target_object
```

所以参考物和目标物分得更清楚。

---

目前模型整体能实现的功能是：

```text
官方/自然语言任务指令
    ↓
Qwen2.5-VL 理解任务
    ↓
生成 GroundingDINO 检测提示词
    ↓
GroundingDINO+SAM 从机器人相机图像中检测和分割物体
    ↓
Qwen2.5-VL 结合任务语义和检测结果
    ↓
判断哪个 object_id 是真正要操作的目标
    ↓
输出结构化场景语义 JSON
```

最终输出大概包含：

```json
{
  "grounding": {
    "selected_object_id": "xxx",
    "selected_label": "yellow box",
    "requires_reobserve": false
  },
  "objects": [
    {
      "object_id": "xxx",
      "label": "yellow box",
      "semantic_role": "target_object",
      "box_xyxy": [x1, y1, x2, y2],
      "centroid_uv": [u, v],
      "mask_area": 1234,
      "dino_score": 0.8,
      "sam_score": 0.9
    }
  ],
  "future_action": {
    "enabled": false,
    "target_object_id": "xxx",
    "skills": []
  }
}
```

也就是说，它现在能做到：

- 识别任务指令里真正要找的目标物体
- 自动生成视觉检测 prompt
- 检测相机画面里的候选物体
- 分割目标区域
- 输出目标框、中心点、mask 面积、置信度
- 区分目标物和参考物
- 给后续动作决策模块预留 `target_object_id`

还不能完整做到的是：

- 不能直接完成抓取
- 不能输出机械臂轨迹
- 不能自动规划移动路径
- 不能从 2D 像素坐标直接得到可靠 3D 抓取位姿，除非继续接深度图、相机内参、TF 坐标变换
- 不能判断物体是否真的已被成功搬运，需要后续状态检测或裁判反馈

所以它现在是一个比较完整的 **VLM视觉语义理解模块**，不是完整机器人闭环控制系统。后面要接的是：

```text
场景语义 JSON
    ↓
动作决策模块
    ↓
导航/底盘控制
    ↓
机械臂抓取规划
    ↓
夹爪控制
    ↓
任务完成状态判断
```